import React from "react";
const usercontext=React.createContext();

export default usercontext;//part-1 creating our context//basically we are creating a context object that we can use to share data across components without having to pass props down manually at every level.
